# mdBook Docs

This folder contains the mdBook source for Glockit docs.

## Local preview

If mdbook is installed:

```bash
cd pages
mdbook serve
```

Open the local URL printed by mdbook.

## Build static site

```bash
cd pages
mdbook build
```

The generated site is written to pages/book.
